'use strict'

describe('Service: timelineSvc', function() {

  // load the service's module
  beforeEach(module('climbingMemo'))

  // instantiate service
  var timelineSvc
  beforeEach(inject(function(_timelineSvc) {
    timelineSvc = _timelineSvc
  }))

  it('should do something', function() {
  })

})
